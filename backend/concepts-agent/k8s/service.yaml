apiVersion: v1
kind: Service
metadata:
  name: concepts-agent
  namespace: learnflow
  labels:
    app: concepts-agent
    tier: backend
    version: v1
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: concepts-agent